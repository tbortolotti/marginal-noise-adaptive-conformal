# Python requirements for the BigEarthNet experiment only
# (experiments/exp_bigearthnet.py and third_party/bigearthnet/).  Python 3.8 --
# third_party/bigearthnet/ targets an older stack, so it needs its own environment.
#
#   conda create -n bigearth python=3.8 && conda activate bigearth
#   pip install -r requirements-bigearthnet.txt
#
# See metadata/dependencies.md.  torchgeo and kornia are imported by
# third_party/bigearthnet/models/bigearthnet_module.py; the exact versions used
# for the paper are not recorded -- install versions compatible with torch 2.4.

numpy==1.24.4
scipy==1.10.1
scikit-learn==1.3.2
pandas==2.0.3
torch==2.4.1
torchvision==0.19.1
pytorch-lightning==2.4.0
timm==1.0.11
hydra-core==1.3.2
omegaconf==2.3.0
gdown==5.2.0
hub==3.0.1          # activeloop "hub" -- reads the pre-processed BigEarthNet dataset
torchgeo           # version unpinned (see note above)
kornia             # version unpinned (see note above)
